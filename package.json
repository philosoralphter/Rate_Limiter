{
  "name": "rate_limiter",
  "version": "0.0.1",
  "description": "Incoming and Outgoing API rate limiter.  Limit other rates if you dare.",
  "main": "server.js",
  "scripts": {
    "lint": "./node_modules/.bin/jshint . --reporter=./node_modules/jshint-full-path/index.js",
    "pretest": "npm run-script lint",
    "test": "./node_modules/.bin/istanbul cover ./node_modules/.bin/_mocha -- --recursive -R spec -r should",
    "posttest": "./node_modules/.bin/istanbul check-coverage && rm -rf coverage"
  },
  "repository": {
    "type": "git",
    "url": "https://philosoralphter@bitbucket.org/hapicoders/ralph-samuel.git"
  },
  "keywords": [
    "API",
    "rate-limiter"
  ],
  "author": "Ralph Samuel",
  "license": "ISC",
  "dependencies": {
    "async": "^0.9.0",
    "hiredis": "^0.1.17",
    "redis": "^0.12.1"
  },
  "devDependencies": {
    "istanbul": "^0.3.5",
    "mocha": "^2.1.0",
    "restify": "^2.8.4",
    "should": "^4.6.0"
  }
}
